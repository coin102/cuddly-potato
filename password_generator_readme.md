# 🔐 密码生成器工具

一个安全、易用的密码生成工具，支持交互式界面和命令行模式，无需安装第三方库。

---

## 一、功能特点

- ✅ **安全随机** - 使用 Python 的 `secrets` 模块生成密码学安全的随机密码
- ✅ **自定义配置** - 支持自定义密码长度和字符类型
- ✅ **强度检测** - 内置密码强度检测功能
- ✅ **批量生成** - 支持一次性生成多个密码
- ✅ **保存文件** - 可将生成的密码保存到文本文件
- ✅ **剪贴板支持** - 自动复制密码到剪贴板（Windows/macOS/Linux）
- ✅ **双模式** - 支持交互式界面和命令行参数

---

## 二、运行环境

- **Python 版本**: 3.6+
- **依赖库**: 仅使用 Python 标准库
- **操作系统**: Windows / macOS / Linux

---

## 三、使用方法

### 1. 交互式模式（推荐）

直接运行程序，进入交互式菜单：

```bash
python password_generator.py
```

### 2. 命令行模式

```bash
# 生成默认 16 位密码
python password_generator.py

# 生成长度为 20 的密码
python password_generator.py -l 20

# 生成 10 个密码并保存到文件
python password_generator.py -n 10 -o passwords.txt

# 生成不含特殊字符的密码
python password_generator.py --no-special

# 排除易混淆字符
python password_generator.py --no-ambiguous
```

### 命令行参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `-l, --length` | 密码长度 | `-l 20` |
| `-n, --count` | 生成数量 | `-n 5` |
| `--no-upper` | 不包含大写字母 | `--no-upper` |
| `--no-lower` | 不包含小写字母 | `--no-lower` |
| `--no-digits` | 不包含数字 | `--no-digits` |
| `--no-special` | 不包含特殊字符 | `--no-special` |
| `--no-ambiguous` | 排除易混淆字符 | `--no-ambiguous` |
| `-o, --output` | 输出文件路径 | `-o pwd.txt` |
| `-i, --interactive` | 强制交互式模式 | `-i` |

---

## 四、运行效果展示

### 交互式模式

```
============================================================
           🔐  密 码 生 成 器  🔐
============================================================

📋 配置密码生成选项
------------------------------------------------------------
密码长度 (4-128, 默认 16): 16

选择包含的字符类型:
包含小写字母 [Y/n]: y
包含大写字母 [Y/n]: y
包含数字 [Y/n]: y
包含特殊字符 [Y/n]: y
排除易混淆字符 (0, O, 1, l, I) [y/N]: n

✅ 配置完成!
按回车键继续...

============================================================
           🔐  密 码 生 成 器  🔐
============================================================

主菜单:
------------------------------------------------------------
  [1] 生成单个密码
  [2] 批量生成密码
  [3] 检测密码强度
  [4] 配置生成选项
  [5] 退出
------------------------------------------------------------

请选择操作 (1-5): 1

============================================================
           🔐  密 码 生 成 器  🔐
============================================================

🎲 生成的密码:
------------------------------------------------------------

  K9#mP2$vL5@nQ8*w

------------------------------------------------------------

📊 密码强度: 非常强
强度: ████
评分: 8/8

详细分析:
  ✓ 长度优秀
  ✓ 包含小写字母
  ✓ 包含大写字母
  ✓ 包含数字
  ✓ 包含特殊字符

📋 密码已复制到剪贴板

按回车键继续...
```

### 密码强度检测示例

```
============================================================
           🔐  密 码 生 成 器  🔐
============================================================

请输入要检测的密码: MyP@ssw0rd123

📊 密码强度分析:
------------------------------------------------------------
密码长度: 13
字符类型: 4/4
强度等级: 非常强
强度: ████
评分: 8/8

详细分析:
  ✓ 长度优秀
  ✓ 包含小写字母
  ✓ 包含大写字母
  ✓ 包含数字
  ✓ 包含特殊字符

💡 改进建议:
  密码强度很好，无需改进！

按回车键继续...
```

### 批量生成示例

```
============================================================
           🔐  密 码 生 成 器  🔐
============================================================

生成数量 (1-50, 默认 5): 5

🎲 生成 5 个密码:
------------------------------------------------------------
 1. aX9#kL2$mN5@pQ8       [强度: 非常强]
 2. vB4&wE7*nJ1!cR3       [强度: 非常强]
 3. hY6$uI8@oP2#lK5       [强度: 非常强]
 4. tG3!rF5@mH7&jK9       [强度: 非常强]
 5. qW2#eS4$dF6@gH8       [强度: 非常强]

是否保存到文件 [y/N]: y
请输入文件名 (默认: passwords.txt): my_passwords.txt
✅ 密码已保存到: my_passwords.txt
```

---

## 五、密码强度评分标准

| 评分 | 强度 | 说明 |
|------|------|------|
| 7-8 分 | 🟢 非常强 | 长度 ≥16，包含 4 种字符类型 |
| 5-6 分 | 🟢 强 | 长度 ≥12，包含 3-4 种字符类型 |
| 3-4 分 | 🟡 中等 | 长度 ≥8，包含 2-3 种字符类型 |
| 1-2 分 | 🔴 弱 | 长度 <8 或字符类型单一 |

### 评分细则

- **长度评分**:
  - ≥16 位: +4 分
  - ≥12 位: +3 分
  - ≥8 位: +2 分
  - <8 位: +1 分

- **字符类型评分**:
  - 小写字母: +1 分
  - 大写字母: +1 分
  - 数字: +1 分
  - 特殊字符: +1 分

---

## 六、安全说明

1. **随机数生成**: 使用 Python 的 `secrets` 模块，该模块专为密码学安全设计
2. **本地运行**: 所有密码在本地生成，不会发送到任何服务器
3. **内存安全**: 密码仅在内存中临时存储，程序退出后自动清除
4. **文件保存**: 保存的密码文件请妥善保管，建议设置文件权限

---

## 七、字符集说明

| 类型 | 字符范围 |
|------|----------|
| 小写字母 | `abcdefghijklmnopqrstuvwxyz` |
| 大写字母 | `ABCDEFGHIJKLMNOPQRSTUVWXYZ` |
| 数字 | `0123456789` |
| 特殊字符 | `!@#$%^&*()_+-=[]{}\|;:,.<>?` |
| 易混淆字符 | `0` `O` `1` `l` `I` |

---

## 八、代码结构

```
password_generator.py
├── PasswordGenerator 类
│   ├── get_character_pool()   # 获取字符池
│   ├── generate()             # 生成单个密码
│   ├── generate_batch()       # 批量生成
│   └── check_strength()       # 检测密码强度
├── PasswordGeneratorCLI 类
│   ├── configure()            # 配置选项
│   ├── generate_single()      # 生成单个
│   ├── generate_multiple()    # 批量生成
│   ├── check_password_strength() # 强度检测
│   └── run()                  # 主循环
├── create_parser()            # 参数解析
└── main()                     # 程序入口
```

---

## 九、使用建议

### 密码长度建议

| 用途 | 建议长度 | 示例 |
|------|----------|------|
| 普通网站 | 12-16 位 | `aB3$kL9@mN2#pQ` |
| 重要账户 | 16-20 位 | `xY7&vB4*wE1!cR5@tG` |
| 金融账户 | 20+ 位 | `hJ8$nK3#lM6@pO9&qR2*sT` |

### 最佳实践

1. **定期更换密码** - 建议每 3-6 个月更换一次重要账户密码
2. **不重复使用** - 每个账户使用唯一的密码
3. **启用双因素认证** - 在支持的服务上启用 2FA
4. **使用密码管理器** - 考虑使用密码管理器存储生成的密码

---

## 十、故障排除

| 问题 | 解决方案 |
|------|----------|
| 无法复制到剪贴板 | 手动复制显示的密码 |
| 保存文件失败 | 检查文件路径和写入权限 |
| 生成的密码不符合要求 | 检查字符类型配置 |

---

## 十一、文件清单

```
d:\clone\
├── password_generator.py          # 主程序
└── password_generator_readme.md   # 使用文档
```

---

**安全提示**: 请妥善保管生成的密码，不要将密码明文存储在不安全的地方！
