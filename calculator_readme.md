# 🧮 简易计算器

一个功能完善的命令行计算器，支持基本运算、数学函数、表达式计算和历史记录功能。

---

## 一、程序功能

- **基本运算**：加、减、乘、除、整除、取余、幂运算
- **数学函数**：sin、cos、tan、sqrt、log、log10、exp 等
- **表达式计算**：支持复杂表达式和括号
- **历史记录**：自动保存计算历史，支持查看和清空
- **双模式**：交互式模式和命令行直接计算

---

## 二、运行环境

- **Python 版本**: 3.8+
- **依赖库**: 仅使用 Python 标准库
- **操作系统**: Windows / macOS / Linux

---

## 三、使用方法

### 1. 交互式模式

直接运行程序，进入交互式界面：

```bash
python calculator.py
```

### 2. 命令行直接计算

```bash
# 计算简单表达式
python calculator.py "2 + 3 * 4"

# 使用数学函数
python calculator.py "sqrt(16) + sin(pi/2)"

# 复杂表达式
python calculator.py "(100 - 25) / 5 + 10 ** 2"
```

### 3. 历史记录管理

```bash
# 显示历史记录
python calculator.py --history

# 清空历史记录
python calculator.py --clear-history
```

---

## 四、支持的运算

### 基本运算符

| 运算符 | 说明 | 示例 | 结果 |
|--------|------|------|------|
| `+` | 加法 | `5 + 3` | `8` |
| `-` | 减法 | `10 - 4` | `6` |
| `*` | 乘法 | `6 * 7` | `42` |
| `/` | 除法 | `15 / 4` | `3.75` |
| `//` | 整除 | `15 // 4` | `3` |
| `%` | 取余 | `15 % 4` | `3` |
| `**` | 幂运算 | `2 ** 10` | `1024` |
| `^` | 幂运算（别名）| `2 ^ 10` | `1024` |

### 数学函数

| 函数 | 说明 | 示例 | 结果 |
|------|------|------|------|
| `sin(x)` | 正弦函数（弧度） | `sin(pi/2)` | `1.0` |
| `cos(x)` | 余弦函数（弧度） | `cos(pi)` | `-1.0` |
| `tan(x)` | 正切函数（弧度） | `tan(pi/4)` | `1.0` |
| `sqrt(x)` | 平方根 | `sqrt(16)` | `4.0` |
| `log(x)` | 自然对数（ln） | `log(e)` | `1.0` |
| `log10(x)` | 常用对数（lg） | `log10(100)` | `2.0` |
| `exp(x)` | 指数函数（e^x） | `exp(1)` | `2.718...` |
| `abs(x)` | 绝对值 | `abs(-5)` | `5` |
| `round(x)` | 四舍五入 | `round(3.7)` | `4` |
| `max(a, b)` | 最大值 | `max(10, 20)` | `20` |
| `min(a, b)` | 最小值 | `min(10, 20)` | `10` |
| `pow(x, y)` | 幂函数 | `pow(2, 8)` | `256` |
| `degrees(x)` | 弧度转角度 | `degrees(pi)` | `180.0` |
| `radians(x)` | 角度转弧度 | `radians(180)` | `3.141...` |

### 常量

| 常量 | 说明 | 近似值 |
|------|------|--------|
| `pi` | 圆周率 π | `3.1415926535...` |
| `e` | 自然常数 e | `2.7182818284...` |

---

## 五、运行效果展示

### 交互式模式

```
============================================================
           🧮  简 易 计 算 器  🧮
============================================================

使用说明:
------------------------------------------------------------
基本运算:
  +  加法        例: 2 + 3
  -  减法        例: 5 - 2
  *  乘法        例: 4 * 5
  /  除法        例: 10 / 2
  // 整除        例: 10 // 3
  %  取余        例: 10 % 3
  ** 幂运算      例: 2 ** 3

高级函数:
  sin(x)    正弦函数      sqrt(x)   平方根
  cos(x)    余弦函数      log(x)    自然对数
  tan(x)    正切函数      log10(x)  常用对数
  abs(x)    绝对值        exp(x)    指数函数

常量:
  pi        圆周率 π
  e         自然常数 e

特殊命令:
  help      显示帮助
  history   显示历史记录
  clear     清屏
  exit/quit 退出程序
------------------------------------------------------------

💡 提示: 输入 'help' 查看帮助，输入 'exit' 退出

>>> 2 + 3 * 4
= 14

>>> sqrt(16) + 5
= 9

>>> sin(pi / 2) + cos(pi)
= 0

>>> 100 / 7
= 14.2857142857

>>> history

📜 计算历史
------------------------------------------------------------
  1. [2024-01-15 10:30:00] ✓ 2 + 3 * 4 = 14
  2. [2024-01-15 10:30:05] ✓ sqrt(16) + 5 = 9
  3. [2024-01-15 10:30:12] ✓ sin(pi / 2) + cos(pi) = 0
  4. [2024-01-15 10:30:18] ✓ 100 / 7 = 14.2857142857
------------------------------------------------------------

>>> exit

👋 感谢使用，再见！
```

### 命令行直接计算

```bash
$ python calculator.py "2 + 3 * 4"
14

$ python calculator.py "sqrt(256)"
16

$ python calculator.py "log(exp(5))"
5

$ python calculator.py "sin(radians(30))"
0.5
```

---

## 六、错误处理

程序内置完善的异常处理机制：

| 错误类型 | 处理方式 | 示例 |
|----------|----------|------|
| 除零错误 | 提示"除零错误" | `10 / 0` |
| 语法错误 | 提示"表达式语法错误" | `2 + * 3` |
| 非法字符 | 提示"表达式包含非法字符" | `2 + abc()` |
| 未知函数 | 提示"未知函数或变量" | `unknown(5)` |
| 结果溢出 | 提示"结果溢出（无穷大）" | `10 ** 1000` |
| 无效结果 | 提示"结果无效（非数字）" | `sqrt(-1)` |

---

## 七、安全特性

- **表达式白名单**：只允许特定字符和函数
- **危险关键字过滤**：自动过滤 import、exec、eval 等危险关键字
- **沙箱执行**：使用受限的 eval 环境，禁用所有内置函数
- **输入验证**：严格的输入检查和预处理

---

## 八、代码结构

```
calculator.py
├── Calculator 类
│   ├── evaluate()             # 计算表达式
│   ├── _preprocess_expression() # 预处理表达式
│   ├── _is_safe_expression()  # 安全检查
│   ├── add_to_history()       # 添加历史记录
│   ├── get_history()          # 获取历史记录
│   └── format_number()        # 格式化数字
├── CalculatorCLI 类
│   ├── print_help()           # 打印帮助
│   ├── show_history()         # 显示历史
│   └── run()                  # 主循环
├── create_parser()            # 参数解析
└── main()                     # 程序入口
```

---

## 九、使用技巧

### 1. 隐式乘法

支持隐式乘法，括号前的数字会自动乘以括号内容：

```
>>> 2(3 + 4)
= 14

>>> (5 + 5)2
= 20
```

### 2. 使用幂运算

支持 `**` 和 `^` 两种幂运算符号：

```
>>> 2 ** 10
= 1024

>>> 2 ^ 10
= 1024
```

### 3. 角度与弧度转换

三角函数默认使用弧度，可以使用转换函数：

```
>>> sin(radians(30))    # 30度角的正弦值
= 0.5

>>> degrees(pi)         # π 弧度转角度
= 180.0
```

### 4. 复杂表达式

支持多层括号嵌套：

```
>>> ((100 - 50) * 2 + 25) / 5
= 25

>>> sqrt(pow(3, 2) + pow(4, 2))
= 5
```

---

## 十、文件清单

```
d:\clone\
├── calculator.py           # 主程序
└── calculator_readme.md    # 使用文档
```

---

**祝您计算愉快！🧮**
